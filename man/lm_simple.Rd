% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lm_simple.R
\name{lm_simple}
\alias{lm_simple}
\title{Fit a Simple Linear Regression Model}
\usage{
lm_simple(formula, data)
}
\arguments{
\item{formula}{A formula of the form \code{y ~ x1 + x2}.}

\item{data}{A data frame containing the variables used in the formula.}
}
\value{
A list of class \code{lm_simple} containing:
\itemize{
\item \code{coef} — Estimated regression coefficients
\item \code{se} — Standard errors of the coefficients
\item \code{fitted.values} — Predicted values
\item \code{residuals} — Model residuals
\item \code{vcov} — Variance–covariance matrix of the estimates
\item \code{call} — The matched function call
}
}
\description{
\code{lm_simple()} fits a linear regression model using the ordinary least squares
(OLS) formula \eqn{(X^T X)^{-1} X^T y}. It is designed as a transparent,
minimal implementation similar to base R's \code{lm()}, but with fewer features.
}
\details{
This function constructs the model matrix using \code{model.matrix()}, computes
OLS estimates directly, and returns fitted values, residuals, and standard
errors. It is intended for educational use and demonstration of how OLS
works internally.
}
\examples{
df <- data.frame(x = 1:10, y = 2*(1:10) + rnorm(10))
fit <- lm_simple(y ~ x, data = df)
fit$coef

}
\seealso{
\code{\link[stats:lm]{stats::lm()}} for the fully featured base R implementation.
}
